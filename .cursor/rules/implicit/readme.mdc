---
description: README structure and content requirements for TypeScript projects
alwaysApply: true
---

# README Requirements for TypeScript Projects

## Required Structure:

```markdown
<div align="center">

# ${PROJECT_DISPLAY_NAME}

${PROJECT_DESCRIPTION}

[![Build Status](https://github.com/${GITHUB_OWNER}/${REPO_NAME}/workflows/Build/badge.svg)](https://github.com/${GITHUB_OWNER}/${REPO_NAME}/actions)
[![License: ${LICENSE}](https://img.shields.io/badge/License-${LICENSE}-yellow.svg)](./LICENSE)
[![Node.js Version](https://img.shields.io/badge/node-%3E%3D${NODE_VERSION}-brightgreen.svg)](https://nodejs.org/)
[![React Version](https://img.shields.io/badge/react-${REACT_VERSION}-blue.svg)](https://reactjs.org/)

[Demo](https://${DEMO_URL}) • [Documentation](./docs) • [Report Bug](https://github.com/${GITHUB_OWNER}/${REPO_NAME}/issues) • [Request Feature](https://github.com/${GITHUB_OWNER}/${REPO_NAME}/issues)

</div>

## 🚀 Features

${PROJECT_FEATURES}

## 🛠 Tech Stack

${PROJECT_TECH_STACK}

## 📋 Prerequisites

- Node.js >= ${NODE_VERSION}
- npm >= ${NPM_VERSION}
- Podman >= 4.0 (for containerized deployment)

## 🚦 Quick Start

### Using Container (Recommended)

```bash
# Clone repository
git clone https://github.com/${GITHUB_OWNER}/${REPO_NAME}.git
cd ${REPO_NAME}

# Run with podman
podman build -f ops/build/development.Containerfile -t ${CONTAINER_NAME}:dev .
podman run --rm -p ${PORT_DEV}:${PORT_DEV} ${CONTAINER_NAME}:dev
```

Visit http://localhost:${PORT_DEV}

### Local Development

```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Open browser to http://localhost:5173
```

## 📁 Project Structure

```
${PROJECT_NAME}/
├── src/
│   ├── components/       # React components
│   │   ├── ui/          # Reusable UI components
│   │   ├── layout/      # Layout components
│   │   └── features/    # Feature components
│   ├── hooks/           # Custom React hooks
│   ├── pages/           # Route pages
│   ├── services/        # API services
│   ├── store/           # Zustand store
│   ├── types/           # TypeScript types
│   ├── utils/           # Utility functions
│   ├── assets/          # Static assets
│   ├── App.tsx
│   └── main.tsx
├── public/              # Public static files
├── ops/                 # Operations (build, test, release)
├── index.html
└── package.json
```

## 🧪 Testing

```bash
# Run unit tests
npm test

# With coverage
npm test -- --coverage

# Run linting
npm run lint

# Type checking
npm run type-check
```

## 🐳 Containerization

### Development

```bash
podman build -f ops/build/development.Containerfile -t ${CONTAINER_NAME}:dev .
podman run --rm -p ${PORT_DEV}:${PORT_DEV} -v $(pwd):/app ${CONTAINER_NAME}:dev
```

### Production

```bash
podman build -f ops/build/master.Containerfile -t ${CONTAINER_NAME}:latest .
podman run --rm -p ${PORT_PROD}:${PORT_PROD} ${CONTAINER_NAME}:latest
```

## 🔧 Configuration

### Environment Variables

Create `.env.local`:

```env
VITE_GITHUB_TOKEN=your_github_token
VITE_API_BASE_URL=https://api.github.com
```

### GitHub Token

Generate a personal access token with `repo` scope for higher API rate limits.

## 🎨 Customization

### Theming

Customize styles in `src/assets/styles/` directory.

### Configuration

Modify component configuration files as needed for your project.

## 🚀 Deployment

### Container Registry

```bash
# Tag for registry
podman tag ${CONTAINER_NAME}:latest ${REGISTRY}/${GITHUB_OWNER}/${CONTAINER_NAME}:latest

# Push to registry
podman push ${REGISTRY}/${GITHUB_OWNER}/${CONTAINER_NAME}:latest
```

### Static Hosting

Build and deploy the dist/ folder:

```bash
npm run build
# Upload dist/ to your hosting provider
```

## 🤝 Contributing

1. Fork the repository
2. Create feature branch (`git checkout -b feature/amazing-feature`)
3. Commit changes (`git commit -m 'Add amazing feature'`)
4. Push to branch (`git push origin feature/amazing-feature`)
5. Open Pull Request

## 📄 License

This project is licensed under the ${LICENSE} License - see the [LICENSE](LICENSE) file for details.

## 🙏 Acknowledgments

${PROJECT_ACKNOWLEDGMENTS}

---

<div align="center">
Made with ❤️ by <a href="https://github.com/${GITHUB_OWNER}">${GITHUB_OWNER}</a>
</div>
```

## Required Sections:

1. ✅ **Centered header** with project name and description
2. ✅ **Badges** (build status, license, versions)
3. ✅ **Demo and links** (demo, docs, issues)
4. ✅ **Features** with checkmarks
5. ✅ **Tech stack** breakdown
6. ✅ **Prerequisites** with versions
7. ✅ **Quick start** (container-first, then local)
8. ✅ **Project structure** visual tree
9. ✅ **Testing** instructions
10. ✅ **Containerization** (podman commands)
11. ✅ **Configuration** (env vars)
12. ✅ **Deployment** instructions
13. ✅ **Contributing** guidelines
14. ✅ **License** (ACE)
15. ✅ **Acknowledgments**

## Key Points:

- **Container-first approach** - Show podman commands first
- **Use actual versions** - Refer to explicit/project-config.mdc for versions
- **No docker** - Always use podman
- **Highlight unique features** - Showcase what makes your project special
- **Explain integrations** - Document API usage and dependencies